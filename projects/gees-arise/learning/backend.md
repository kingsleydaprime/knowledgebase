# Backend — Gees Arise

Skeleton for now — filled in as the API/server-side logic gets built.

---

## To cover as we build

- [ ] Next.js App Router server-side pieces (Route Handlers / Server Actions) used to talk to Supabase
- [ ] Where business rules live: DB constraint vs. server-side check vs. client-side check (see `learning/sys-design.md` §5 for the reasoning)
- [ ] Resend integration for nudge emails
- [ ] Validating/authorizing requests (only a circle member can flag/vouch on that circle's proofs)
