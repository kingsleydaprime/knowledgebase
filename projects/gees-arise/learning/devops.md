# DevOps — Gees Arise

Skeleton for now — filled in as deployment/infra work happens.

---

## To cover as we build

- [ ] Vercel deployment basics (connecting a repo, environment variables, preview deployments per branch/PR)
- [ ] Managing secrets (Supabase keys, Resend API key) across local `.env` vs. Vercel project settings
- [ ] `pg_cron` as an infra concern — a scheduled job running inside the database itself, not a separate server
