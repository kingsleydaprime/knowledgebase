# Number Bases — Introduction

Overview of positional numeral systems: decimal (base 10), binary (base 2), octal (base 8), hexadecimal (base 16). Each base uses different digits but the same positional rule.
