# Frontend

**Scaffold — folder structure and planned reading order only, most of the actual notes don't exist yet.** Framework-agnostic concepts already live in [[concepts/02-frontend/README|concepts/frontend/]] — read that first. This folder is the framework-specific layer on top.

## Structure

1. [[frontend/01-react/README|01-react/]] — **[Intermediate]** — currently [[01-react/01-react-error-handler|react-error-handler]]. Planned: hooks in depth, data fetching patterns, error boundaries, testing (React Testing Library).
2. `02-next/` — **[Intermediate → Advanced]** — nothing written yet. Planned: App Router fundamentals, server vs client components, data fetching/caching, route handlers, deployment (Vercel).

`vue/` exists as an untouched, empty placeholder — not part of the current plan.

## Related
- [[concepts/02-frontend/README|frontend concepts]] — the framework-agnostic ideas these tracks implement
