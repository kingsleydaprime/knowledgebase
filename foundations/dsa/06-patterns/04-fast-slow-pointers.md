# Pattern: Fast & Slow Pointers

Also called Floyd's Tortoise and Hare. Walk two pointers through a [[04-linked-lists|linked list]] (or any "follow a pointer/function repeatedly" structure) at different speeds — one step at a time vs two steps at a time. If there's a cycle, the fast pointer eventually laps the slow one and they meet; if there isn't, the fast pointer simply reaches the end.

## When to use it

Detecting a cycle, or more generally, detecting that a sequence of "next" steps repeats/loops — which comes up even outside linked lists (see the Happy Number example below).

## How it works

```python
def has_cycle(head):
    slow = fast = head
    while fast and fast.next:
        slow = slow.next          # one step
        fast = fast.next.next     # two steps
        if slow == fast:
            return True
    return False
```

```
1 -> 3 -> 5 -> 7 -> 9
          ^_________|          (9 points back to 5 — a cycle)

slow moves one node at a time, fast moves two —
fast is on a "shorter loop" relative to slow once both are inside the cycle,
so it's guaranteed to catch up and land on the same node as slow eventually.
```

Why they're guaranteed to meet: once both pointers are inside the cycle, the *gap* between them shrinks by exactly one node every step (fast closes distance faster than the cycle can lengthen the gap) — so the gap must eventually hit zero, meaning they occupy the same node.

## Why not just use a hash set of visited nodes?

You can — `if node in visited: return True` — and it's simpler to reason about. But it costs O(n) extra space. Fast & slow pointers get the same O(n) time with O(1) space, which is the entire reason this pattern is worth knowing rather than just always reaching for a set.

## Complexity

O(n) time, O(1) space — the space savings over the hash-set approach is the whole point of this pattern.

## Beyond linked lists

The same idea detects cycles in *any* sequence generated by repeatedly applying a function to a value — e.g. "Happy Number" defines a sequence via digit-squares, and some starting numbers cycle forever instead of reaching 1; treating "next number in the sequence" as a linked-list `.next` and running the exact same fast/slow logic finds that cycle.

## Practice problems
1. Linked List Cycle (LeetCode #141)
2. Happy Number (LeetCode #202) — same algorithm, no linked list in sight
3. Find the Duplicate Number (LeetCode #287) — treats array values as implicit `next` pointers

## Related
- [[04-linked-lists|linked-lists]]
- [[05-linked-list-reversal|linked-list-reversal]]
